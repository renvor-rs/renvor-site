# Renvor site — repository ignore rules
#
# This file previously stated that the repository was PRIVATE and that the site was NOT
# deployed. **Both were false**: this repository is public, and the landing site has been
# deployed since 2026-08-17. Project-status claims are removed rather than corrected —
# an ignore file is not where deployment status belongs, and a status line here goes stale
# silently. Deployment state is recorded where it is maintained, in the framework
# repository's governance records.
#
# Scope: keep dependencies, build output, caches, local state, and credentials out of Git.
# A file being ignored is not the same as a file being safe to create — see the credentials
# section, which is a backstop and not a licence to keep secrets in the working tree.

# --- Dependencies ---
# pnpm is the package manager (packageManager: pnpm@11.21.0). pnpm-lock.yaml is COMMITTED
# on purpose: this is a deployable application, not a reusable library, so the exact
# resolved tree is part of what gets reviewed and shipped.
node_modules/
.pnpm-store/
.pnpm-debug.log*

# --- Next.js build output and caches ---
# `out/` is the static export produced by `pnpm run build`; `.next/` is the build cache.
# Committing either would mean reviewing minified output and would let the built site drift
# from the source that supposedly produced it.
out/
.next/

# The Content-Security-Policy and the server configuration are DERIVED from `out/` at build
# time (scripts/generate-csp.mjs). They are deliberately not committed: one inline script is
# the RSC flight payload, whose hash changes with any component edit, so a checked-in policy
# would be stale after the next copy change — and a stale strict policy is what gets weakened
# to make a site work again. The image generates and carries its own.
sws.generated.toml

# --- Inspection and test captures ---
# Screenshots taken while checking themes, motion, and layout. They are evidence of a
# point-in-time check, not source, and they go stale silently. Six such files existed in
# the pre-migration directory and were deliberately not copied into this repository.
inspection/
*.screenshot.png
playwright-report/
test-results/
.playwright-mcp/

# SBOMs and scan reports are produced by CI from the image and the lockfile. They describe
# one build, so committing one would be committing a claim that goes stale silently.
sbom/

# --- Environment and credentials (never commit) ---
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
credentials
credentials.*
secrets/
*.secret

# --- Editor and IDE state ---
.idea/
.vscode/
*.iml
*.swp
*.swo

# --- Local development-tool state ---
# Restored to the TRACKED ignore file on 2026-08-19, after two independent automated review
# passes flagged its removal.
#
# These were moved to `.git/info/exclude` on the reasoning that local-only material does not
# belong in a rule imposed on everyone who clones. That reasoning does not survive contact with
# two facts: this repository is **public**, and `.git/info/exclude` is **per-clone**. A second
# clone — another machine, another contributor, CI — inherits none of it, and a single broad
# `git add .` there publishes local tool state irreversibly.
#
# `.dockerignore` already excludes exactly these paths as a confidentiality boundary. A tree
# where the Docker build context is protected and the Git index is not is not a considered
# position, it is an inconsistency.
#
# `.git/info/exclude` keeps them too. This is belt and braces, deliberately.
.claude/
.claude-flow/
.specify/
.remember/

# --- Operating-system artifacts ---
.DS_Store
Thumbs.db

# --- Logs and temporary files ---
*.log
npm-debug.log*
yarn-error.log*
tmp/
*.tmp

# --- TypeScript incremental build info ---
*.tsbuildinfo
